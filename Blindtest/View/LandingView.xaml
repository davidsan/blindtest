<UserControl x:Class="Blindtest.View.LandingView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
        <StackPanel Orientation="Vertical" Margin="120,50,100,80">
            <Label Content="Write your nickname"  FontSize="30" />
            <StackPanel Orientation="Horizontal" Margin=" 0,20">
                <Label Content="Nickname : "  FontSize="20"/>
                <TextBox Name="nickTextBox" TextWrapping="NoWrap" Text="{Binding Username}" Width="180"  FontSize="20" AcceptsReturn="False" />
            </StackPanel>
        <Button Name="goButton"  Content="Let's go !!!" Margin="0,15" Height="40"  Command="{Binding BtnGo}"/>

    </StackPanel>

    
</UserControl>
